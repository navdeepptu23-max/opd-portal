databases:
  - name: opd-portal-db
    databaseName: opdportal
    user: opdportal

services:
  - type: web
    name: opd-portal
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: opd-portal-db
          property: connectionString
